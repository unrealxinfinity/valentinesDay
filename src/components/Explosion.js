import { useEffect } from "react";

export default function Explosion({anchorObject=null}){
    const anchorObjectRef = anchorObject;
    const getOrigin = (anchorObject)=>{

    }
    
    useEffect(()=>{
        
    },[])
    
}